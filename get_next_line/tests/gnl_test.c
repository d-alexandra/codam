
int main(int ac, char **av)
{
    char  *line;
    int   fd1;
    int gnl = 0;
    int count;
    count = 1;
    fd1 = open("original.txt", O_RDONLY);
    printf("===================================================================================================================================================\n");
    while(count > 0) 
    {
        count = get_next_line(fd1, &line);
        printf("%d %s\n", count, line);
        if(count >= 0 && line != NULL)
            free(line);
        printf("---------------------------------------------------------------------------------------------------------------------------------------------------\n");
    }
    printf("\n===================================================================================================================================================\n");
    close(fd1);
    return 0;
}
